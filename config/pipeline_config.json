{
  "version": "2",
  "pipeline": {
    "source": {
      "http": {
        "authentication": {
          "http_basic": {
            "username": "{{oci_secrets:opensearch_creds:username}}",
            "password": "{{oci_secrets:opensearch_creds:password}}"
          }
        }
      }
    },
    "processor": [
      {
        "grok": {
          "match": {
            "message": ["%{COMMONAPACHELOG}"]
          }
        }
      }
    ],
    "sink": [
      {
        "opensearch": {
          "hosts": ["https://opensearch-cluster.ap-sydney-1.opensearch.cluster.oci.oraclecloud.com:9200"],
          "username": "{{oci_secrets:opensearch_creds:username}}",
          "password": "{{oci_secrets:opensearch_creds:password}}",
          "index": "my-index"
        }
      }
    ],
    "buffer": {
      "persistence": {
        "enabled": true
      }
    }
  }
}
