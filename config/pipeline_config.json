{
  "version": "2",
  "pipeline": {
    "source": {
      "http": {
        "authentication": {
          "http_basic": {
            "username": "{{oci_secrets:opensearch_creds:username}}",
            "password": "{{oci_secrets:opensearch_creds:password}}"
          }
        }
      }
    },
    "processor": [
      {
        "grok": {
          "match": {
            "message": ["%{COMMONAPACHELOG}"]
          }
        }
      }
    ],
    "sink": [
      {
        "opensearch": {
          "hosts": ["{{oci_secrets:opensearch_endpoint:value}}"],
          "username": "{{oci_secrets:opensearch_creds:username}}",
          "password": "{{oci_secrets:opensearch_creds:password}}",
          "index": "my-index"
        }
      }
    ],
    "buffer": {
      "persistence": {
        "enabled": true
      }
    }
  }
}
